we need to figure out how to add a front end to this project. 

that way someone can use the project without having to use the command line.

for now, its just me. because i need this hosted on a server so that i can shut my lpatop and not have the scraping process end. 

I know bun has a server.. can i host this on cloudflare? 